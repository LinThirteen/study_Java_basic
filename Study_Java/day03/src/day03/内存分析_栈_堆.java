package day03;

//java虚拟机的内存可以分为三个去:栈(statck),堆(heap),方法去(method area)

//栈：用来表示方法执行的内存模型，栈属于线程私有的，不能实现线程间的共享

//堆：用来存储创建好的对象和数组（数组也是对象），jvm只有一个堆，被所有线程共享。

//方法区（在堆里面）：jvm只有一个方法区，被所以线程共享！，只用于存储永远不变类，字符串，常量相关的信息




public class 内存分析_栈_堆 {

}
